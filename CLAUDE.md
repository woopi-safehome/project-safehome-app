# CLAUDE.md — project-safehome-app

**이 저장소에서의 역할과, 관행과 달라 모르면 틀리는 것들.**
관행대로 쓴 코드는 문법도 구조도 멀쩡하다. 그래서 어기면 대개 에러 없이 잘못 동작한다.
스택·구조·실행 명령·라우트·함정의 구체적인 내용 → [`README.md`](README.md)

---

## 역할과 경계

화면이다. 사용자가 보고 조작하는 것이 전부다.

**여기서 하지 않는 일**

- **AI 분석 서버를 직접 부르지 않는다.** 통신 대상은 백엔드 하나뿐이다.
- **판정을 다시 계산하지 않는다.** 위험도·검증 결과는 서버가 정하고 앱은 받아서 보여준다.
  앱에서 계산하면 서버와 답이 갈라지고, 어느 쪽이 맞는지 알 수 없게 된다.
- **서버 쪽 비밀값을 앱에 두지 않는다.** 배포된 앱은 뜯어볼 수 있다.
  번들에 들어가는 키는 애초에 공개된 것으로 다룬다 — 소셜 로그인 네이티브 키가 그렇다.

**혼자 정하지 않는 것**

- 서버 응답 형식에 대한 가정 (계약이다)
- 빌드 도구 버전을 고정해 둔 설정을 푸는 것

## 시작 전

1. [`README.md`](README.md) — **작업 레시피** 표에서 건드릴 파일과 순서를 확인
2. 공통 인프라 계층과 화면 계층의 각 `README.md`

## 필수 절차

- **직렬화 모델을 고쳤으면 즉시 코드 생성을 돌린다.** 생성물이 옛 스키마로 남아
  **JSON 파싱이 런타임에 조용히 깨진다.** 명령은 README에 있다.
- **실행은 환경 정의 파일을 주입하는 스크립트로 한다. 빌드 스크립트는 없으므로 빌드할 때도 같은 주입 인자를 넘긴다.**
  주입 없이 실행·빌드하면 키가 빈 값으로 컴파일되어 **빌드는 성공하고 로그인만 실패**한다. 원인을 찾기 어렵다.
- **소셜 로그인 키는 넣을 곳이 하나가 아니다.** 한 곳만 채우면 빌드는 되고 로그인에서 실패한다.
  어디에 무엇을 넣는지는 `dart_defines/README.md`가 갖는다.
- **새 화면을 만들면 라우터에 등록한다.** 파일만 만들어도 라우트는 생기지 않는다.
  제약 테스트가 등록 여부를 검사하므로 빠뜨리면 테스트에서 걸린다. 빌드만으로는 걸리지 않는다.
- **진입점은 플레이버만 정하고 공통 초기화로 넘긴다.** 진입점에 초기화를 따로 적으면 한쪽에서 빠뜨려도
  빌드는 되고, 그 빌드에서만 로그인·푸시가 조용히 죽는다. 제약 테스트가 모든 진입점이 공통 초기화를 거치는지 검사한다.
- 상태의 수명에 맞는 종류를 고른다 — 앱 전역 / 화면 한정 / 식별자별.
- **테스트를 추가하거나 지우면 문서의 "무엇이 잡히는가" 서술을 함께 본다.**
  이 저장소의 문서는 어겼을 때 신호가 나는지 여부를 적는다. 방어 장치가 바뀌면
  그 서술이 조용히 거짓이 된다 — 코드가 아니라 검증 체계가 바뀌어서 생기는 드리프트다.
  제약 테스트를 만들면 **실패 메시지에** 이유와 근거 문서를 적고, **검사할 대상을 하나도 찾지 못하면 실패하게** 만든다.
  근거 문서가 없으면 어긴 순간에 문서가 도착하지 않고, 대상이 비면 아무것도 보지 않은 채 통과한다.
- **코드를 고치면 가장 가까운 `README.md` 를 함께 본다.** 구조·관통 흐름·불변식·실패 규칙·설계 결정이
  바뀌었으면 같은 커밋에서 고친다. 이름만 바꿨거나 내부 구현만 손봤다면 그대로 둔다.
  문서의 형식은 제약 테스트가 지킨다 — 링크와 인용한 절이 실제로 있는지, 계약 절의 표식,
  머리글과 문서 지도, 제약 테스트의 근거 문서. **문장이 코드 동작에 대해 사실인지는 테스트가 보지 못한다.**
  그것이 이 절차의 몫이다.
- **끝났다고 보고하기 전에 `scripts/verify.sh` 를 돌린다.** CI 가 같은 파일을 실행한다 —
  명령이 다르면 로컬 통과가 CI 통과를 뜻하지 않는다. 푸시 뒤에는 `scripts/ci_status.py` 로 원격 결과를 본다.
  보고는 **로컬 검증 / 원격 결과 / 확인하지 못한 것**으로 나눈다. 실행해 보지 않은 동작은 "확인 안 됨"이다.

## 금지사항

- **서버 통신을 화면에서 직접 하지 않는다.** 클라이언트 계층 한 곳에만 모은다.
- **서버 응답 형식을 임의로 가정하지 않는다.** 원본은 서버 저장소 README의 계약 절이 갖는다.
  확인할 수 없으면 실제 응답으로 확인하고, **모르는 필드가 와도 무시하고 동작**해야 한다.
  서버가 앱보다 먼저 배포된다.
- **서버가 정하는 열거값을 문자열로 흩뿌리지 않는다.** 한 곳에 타입으로 정의해 재사용한다.
- **생성된 파일을 직접 고치지 않는다.** 다음 코드 생성에서 조용히 덮어써진다.
- **네이티브 빌드에 버전을 고정해 둔 설정을 지우지 않는다.** 불필요해 보이지만,
  일부 플러그인이 최신 툴체인과 충돌해 빌드가 깨진다. 왜 그 값인지는 README에 있다.
